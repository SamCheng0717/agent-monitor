## 2026-04-30

### 制定代码审查标准与流程
- 全面调研了项目结构：monitor.py (618行)、advisor.py (1044行)、web/app.py、tests/
- 识别了代码质量基线问题：硬编码路径、类型注解缺失、异常处理笼统、缺乏自动化工具链等
- 创建了 `docs/code-review-standards.md`，包含：
  - 审查流程（4 阶段：自检 → 自动化 → 同行审查 → 安全审查）
  - 审查角色与职责（开发者/审查者/维护者）
  - 编码标准（命名、文件组织、import、注释、错误处理）
  - Python 专项标准（类型注解、格式化、配置管理、并发、测试）
  - LLM 应用专项标准（Prompt 安全、多模型协同、评估通路）
  - 四级 Checklist（通用/Python/LLM/安全，共 32 项）
  - 自动化工具链配置（ruff、mypy、pre-commit、CI）
  - 代码健康度指标目标值

### 依据审查标准实操整改
- **P0**：修复 `test_monitor.py` 硬编码路径 `E:/cs-agent` → 动态 `Path(__file__).parent.parent`
- **P0**：修复 `test_advisor.py` 相对路径 `.` → 动态绝对路径
- **P1**：创建 `.env.example` 包含所有环境变量模板（DeepSeek/Local/Dify/DB/钉钉）
- **P1**：修复 5 处 `except Exception` 为精确类型（`json.JSONDecodeError` / `KeyError` / `TypeError` / `AttributeError`）
  - `monitor.py`: `detect_conversion`(2), `score_conversation`
  - `advisor.py`: `_judge_behavior`, `_judge_violates`, `generate_candidate`
- **P2**：确认项目中公共函数类型注解覆盖率已达标（`-> 40 tests all green`）
